import java.io.FileInputStream;
import java.io.FileOutputStream;
import java.io.IOException;

public class LineData {
  private String stringComponent;
  private //date//;
  #develop a date module that consists of a data of year, month, day, and the # of the note.

  public LineData(String stringInput, int YYYYMMDD) {
  #a basic line of data, that is planned to be the building block of subsequent data structures.
  #the current structure does not limit what the this class is planned to be.
  #elements such as pointing might be integrated for accurate representation of real world data onto a flat note.

    this.stringComponent = stringInput;

  }

  public getStringComponent() {
    return this.stringComponent;
  }
  public dataHandle() {
    #to be finished
    return ;
  }
  public protectedModifyStringComponent(String requested, ?data - acception)
  
-+}